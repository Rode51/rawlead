# Portfolio labs — static Next export. TLS: certbot.
# sudo mkdir -p /var/www/labs.rawlead.ru
# sudo ln -sf /opt/rawlead/deploy/nginx/labs.rawlead.ru.conf /etc/nginx/sites-enabled/
# sudo nginx -t && sudo systemctl reload nginx
# sudo certbot --nginx -d labs.rawlead.ru

server {
    listen 80;
    listen [::]:80;
    server_name labs.rawlead.ru;

    root /var/www/labs.rawlead.ru;
    index index.html;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    location /_next/static/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# After certbot, it adds a 443 server block. For manual TLS, duplicate api.rawlead.ru SSL pattern:
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name labs.rawlead.ru;
#     ssl_certificate     /etc/letsencrypt/live/labs.rawlead.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/labs.rawlead.ru/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     root /var/www/labs.rawlead.ru;
#     index index.html;
#     location / { try_files $uri $uri/ $uri.html /index.html; }
#     location /_next/static/ { expires 30d; add_header Cache-Control "public, immutable"; }
# }
