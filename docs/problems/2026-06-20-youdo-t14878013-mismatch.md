# YouDo t14878013 — карточка ≠ биржа

**Дата:** 2026-06-20  
**URL:** https://youdo.com/t14878013  
**Lead:** `id=8256` · `source=youdo`

## Симптом

В ленте: «Закрытый парсинг», до 400₽, web scraping/python. На YouDo — другое содержание (owner smoke).

## Факты (VPS SQL + detail fetch 2026-06-20)

| Поле | БД |
|------|-----|
| title | Закрытый парсинг |
| budget_text | до 400 ₽ |
| body | listing snippet (без detail page) |
| task_summary | L1 по snippet |
| is_visible | было `true` → delist |
| detail fetch live | **FAIL** (`detail_ok=false`, HTML 0) |

`GET /v1/feed?source=youdo&limit=100` — карточка **не** в выдаче после delist.

## Причина

1. `_resolve_ingest_body` для YouDo **пропускал** detail fetch, если `listing_snippet >= YOUDO_DETAIL_MIN_CHARS` (300).
2. L1/L2 шли по одному листинг-snippet без проверки detail page.
3. При fail detail позже guard `detail:short` не срабатывал (`detail_ok=None`).

Класс бага: **listing-only ingest** при длинном snippet → галлюцинация L1 / несовпадение с биржей.

## Фикс

| # | Что |
|---|-----|
| 1 | `lead_pipeline.py` — убран early-return «snippet длинный → не fetch detail» для YouDo |
| 2 | VPS: `UPDATE leads SET is_visible=FALSE, delist_reason='youdo_detail_mismatch' WHERE url ILIKE '%14878013%'` |
| 3 | pytest: `test_youdo_long_listing_snippet_still_fetches_detail` |

**Deploy:** API/radar (`lead_pipeline.py`) · Next не трогали.

## Проверка

- `pytest tests/test_o223_youdo_guard.py -q`
- owner: карточка t14878013 не в `/lenta` (YouDo filter)
