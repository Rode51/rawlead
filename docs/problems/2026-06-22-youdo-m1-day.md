# YouDo — сбой M1-день (2026-06-22)

**Статус:** частично закрыто (restore snippets) · **открыто:** detail ServicePipe · ingest plateau  
**Родитель:** [`2026-06-16-youdo-antibot-browser.md`](2026-06-16-youdo-antibot-browser.md) · golden O268

---

## Симптомы (владелец)

1. YouDo пропал из фильтров / ленты (O281 gate + audit 521 hidden).
2. Фильтры TG / atomic apply — отдельный трек FEED-FILTER (✅ hotfix).
3. «Несколько дней стабильно» (listing `parsed=50` с O268) — но **22.06** регресс в продукте.

---

## Факты VPS (`data/radar_site.log`, probe Lead 2026-06-22)

### Listing (парсер листинга)

| Время UTC | `parsed` | `new` | Комментарий |
|-----------|----------|-------|-------------|
| 00:17–01:08 | 50 | 19–26 | нормальный ingest |
| 01:36+ | 50 | **0** | `youdo:ingest done=0 new=0` — все id уже в БД |
| 12:50 | 50 | 0 | листинг жив, новых нет |

**Вывод:** listing **не упал** 22.06 · plateau `new=0` = дедуп, не обязательно инцидент.

### Detail / L1

| | |
|---|---|
| `detail:short` | последние строки **22.06 01:36–03:00** (до/во время O281) |
| `YOUDO_DETAIL_MIN_CHARS=0` | deploy + restore **4219** visible — продуктовый фикс |
| ServicePipe detail | по-прежнему блок · body 60–100 chars |

### Antibot (листинг)

```
12:59–13:01  html_len=1701  antibot_hit=1  status=antibot
```

Чередование 1700b shell — **watch**, не полный outage (в том же окне `parsed=50` в соседних циклах).

---

## Root cause (продукт)

| # | Причина | Fix |
|---|---------|-----|
| 1 | O281 gate + audit скрыли все YouDo | `YOUDO_DETAIL_MIN_CHARS=0` + `restore_youdo_visible_vps.py` ✅ |
| 2 | ServicePipe detail никогда не давал full TZ на VPS | § **YOUDO-DETAIL-BREAKTHROUGH** P0 |
| 3 | `new=0` днём | ожидаемо при полном листинге без новых id |

---

## DoD расследования (для MiMo coder)

- [ ] **E click-through detail** + trace `stage=click_detail` (§ `CODER_PROMPT`)
- [ ] Kill-switch `YOUDO_CLICK_DETAIL=0` · fallback `goto`

---

## Бэкап перед click-through (2026-06-22)

| Артефакт | Путь VPS |
|----------|----------|
| **Pre click-through** | `data/backups/youdo_profile_pre_clickthrough_2026-06-22.tar.gz` (64M, sha256 `ae806424…98f9`) |
| **Golden O268** | `data/backups/youdo_profile_g2_2026-06-19.tar.gz` |

**Откат:**
```bash
cd /opt/rawlead/data
sudo -u rawlead tar -xzf backups/youdo_profile_pre_clickthrough_2026-06-22.tar.gz
systemctl restart rawlead-radar
```

**Маршрут:** MiMo `coder` · § `CODER_PROMPT` YOUDO-DETAIL-BREAKTHROUGH
