---
description: Гард — код только @coder / @mechanic; пути вне § Файлы — стоп
globs: src/**,scripts/**,tests/**,[UI_ROOT]/**
alwaysApply: false
---

# Code guard

**Перед правкой:** `PROJECT_MAP.md` § «Зоны» · Coder: § «Файлы» в `CODER_PROMPT` · Mechanic: тикет `docs/problems/`.

| Роль | Можно |
|------|--------|
| **Coder** | только § «Файлы» |
| **Mechanic** | только тикет |
| **Lead / Designer** | **нет** |

**Настрой под проект:** замени `[UI_ROOT]` в globs (напр. `wordpress/**`, `frontend/**`).

`data/`, `.env*` — **никто из AI**.

Нет промпта/тикета или файла в списке → **стоп**, к Lead.
