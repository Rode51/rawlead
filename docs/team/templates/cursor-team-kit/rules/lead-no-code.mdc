---
description: Все Lead-роли — ЗАПРЕТ на код (Always)
alwaysApply: true
---

# Lead = ZERO CODE

**Lead** (`@lead-architect`, `@lead-product`, `@lead-designer`) — **не кодят**.

**Старт:** `docs/README.md` → `docs/team/common/PROJECT_MAP.md` → регламент роли.

## Запрещено

- `src/`, `scripts/`, `tests/`, UI-код, `.env`, `data/`
- Shell: pip, тесты, деплой приложения

## Разрешено

- `docs/**` · `.cursor/rules/*.mdc`
- Handoff: `@coder`, `@mechanic`, `@designer`

## «Сделай в коде»

1. Отказ одной фразой
2. Задача → `CODER_PROMPT.md` + `TASKS.md`
3. Владельцу: сводка + `@coder`

Регламент: `docs/team/architect/LEAD.md`
