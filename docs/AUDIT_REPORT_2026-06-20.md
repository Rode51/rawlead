# Аудит проекта uisness — Token Bloat & Health Report

**Дата:** 2026-06-20  
**Цель:** Найти что мешает проекту, жрёт токены агентов, дублируется, устарело.

---

## КРИТИЧНО — Секреты в открытом виде

**Файл `.env` содержит пароли в plaintext:**
- VPS SSH пароль
- Neon DATABASE_URL с паролем
- Telegram API hash
- ~50 прокси-аккаунтов (логины/пароли)
- FL/Kwork пароли, OPS_PASSWORD
- Yandex Metrika OAuth token

**Действие (уточнение 2026-06-20):** `.env` уже в `.gitignore` + `.cursorignore` + MiMo deny — **в Git не уходит**. Ротировать пароли **только при реальной утечке** (коммит, чат, скрин, взлом ПК). Иначе — соблюдать правила в [`FOR_YOU.md`](FOR_YOU.md) § `.env`.

---

## 1. ДУБЛИКАТЫ ДОКУМЕНТОВ

### FILTERS — 3 файла, одинаковое содержимое

| Файл | Строк | Проблема |
|------|-------|----------|
| `docs/ops/FILTERS.md` | 163 | Оригинал |
| `docs/ops/FILTERS_LEGACY.md` | 163 | Побайтовая копия |
| `docs/ops/FILTERS_SITE.md` | 182 | Супермножество |

**Риск:** Правят один — другие отстают.  
**Решение:** Один файл с секциями по профилям.

### Источники (two-contour модель)

| Файл | Строк | Статус |
|------|-------|--------|
| `docs/ops/SOURCES_SAAS.md` | 4 | Заглушка "модель отменена" |
| `docs/archive/SOURCES_SAAS.md` | 40 | Мёртвый контент |

**Решение:** Удалить `ops/SOURCES_SAAS.md`.

### PROJECT_MAP — 2 версии одной карты

| Файл | Строк |
|------|-------|
| `docs/team/common/PROJECT_MAP.md` | 242 |
| `docs/team/common/PROJECT_MAP_VISUAL.md` | 124 |

**Решение:** merger визуал в основной файл.

---

## 2. УСТАРЕВШИЕ / РАЗдутые документы

| Файл | Строк | Лимит | Проблема |
|------|-------|-------|----------|
| `OWNER_INTENT.md` | **2,194** | ~500 | Журнал + решения смешаны. Superseded решения (O12, O102, O105, O107) занимают место. |
| `LEAD_PRODUCT_PROMPT.md` | **2,446** | ~300-500 | Исторические секции рядом с активными. |
| `CODER_PROMPT.md` | **856** | ~120 | Закрытые задачи не заархивированы. |
| `STATUS.md` | **443** | ~80 | В 5 раз превышает собственный лимит. Детали деплоев O262* не заархивированы. |
| `FOR_YOU.md` | **485** | ~150-200 | Накопились прокси, Metrika, чеклисты. |
| `feed-cabinet-mvp.md` | **1,367** | ~300-500 | Спека по компонентам, нужно разбить. |

**Решение:** Архивировать закрытые секции. Целевой размер: `STATUS.md` ≤80, `CODER_PROMPT.md` ≤120, `OWNER_INTENT.md` ≤500.

---

## 3. МЁРТВЫЕ ДОКУМЕНТЫ (ноль ссылок)

| Файл | Строк | Доказательство |
|------|-------|----------------|
| `docs/archive/AUDIO_COMPOSITION_MVP_RESEARCH.md` | 869 | Нигде не упоминается |
| `docs/archive/CURSOR_DEEP_RESEARCH_2026.md` | 307 | Нигде не упоминается |
| `docs/archive/RESEARCH_PROMPT_TG_CHATS.md` | 45 | Нигде не упоминается |

**Причина:** Архивные файлы остались после завершённых исследований.

**Решение:** Удалить или переименовать в `_deprecated_*`.

---

## 4. ДУБЛИКАТЫ В КОДЕ (Python src/)

### Мёртвые модули (не импортируются нигде)

| Файл | Строк | Описание |
|------|-------|----------|
| `habr_freelance_parser.py` | 67 | Дубль `habr_career_parser.py`. Нигде не импортируется. **→ удалить (A7)** |
| `cursor_proxy_relay.py` | 263 | **Живой:** `scripts/cursor_proxy_relay.py` грузит `src/` по path · **не удалять** |
| `process_guard.py` | 407 | **Живой:** `radar_control`, desktop `.bat`, spawn workers · **не удалять** |

### Дублирование `_feed_today_count` в api_server.py

4 варианта одной функции (строки 641-808):
- `_feed_today_count_standalone()`
- `_feed_today_count()`
- `_feed_today_count_cached()`
- `_feed_today_count_standalone_cached()`

**Решение:** Один метод с параметрами.

### Импорт приватных функций между модулями

```python
from src.telegram_control import _send_to_chat        # support_tickets.py
from pg_storage import _ensure_user_tags_columns       # api_server.py
```

**Решение:** Сделать публичными или рефакторить интерфейс.

### Устаревший FastAPI паттерн

```python
@app.on_event("startup")   # deprecated в FastAPI 0.93+
@app.on_event("shutdown")
```

**Решение:** Заменить на `lifespan` context manager.

---

## 5. ГИГАНТСКИЕ ФАЙЛЫ (нужен сплит)

| Файл | Строк | Проблема |
|------|-------|----------|
| `api_server.py` | **3,868** | Монолит: auth + feed + cabinet + quiz + admin + billing + tickets. 40% всего кода. |
| `exchange_browser_fetch.py` | **3,668** | Браузерная автоматизация для всех бирж в одном файле. |
| `exchange_proxy.py` | **1,594** | Управление прокси + HTTP-запросы в одном. |
| `ai_analyze.py` | **2,404** | L1/L2 анализ + бюджет + категоризация + драфты. |
| `owner_admin.py` | **2,398** | Админка с HTML-шаблонами внутри Python. |

**Топ-5 файлов = ~13,898 строк = ~40% всего исходного кода.**

---

## 6. МУСОР В scripts/

### 181 deploy-скрипт (одноразовые)

Паттерн: `deploy-o{N}-vps.py` — каждый для своей задачи, не переиспользуются. 433 KB мёртвого веса.

### 98 отладочных скриптов `_probe_*`, `_smoke_*`, `_check_*`

Одноразовые диагностики, больше не нужны.

### 5 файлов `_tmp_*` — явный мусор

### 6 файлов `.out` / `.txt` / `.html` — вывод отладки

**Решение:** Архивировать все в `scripts/_archive/` или удалить.

---

## 7. ПРОЕКТЫ-ПРИЗРАКИ

| Директория | Статус | Доказательство |
|------------|--------|----------------|
| `desktop/` (Tauri) | **Мёртвый** | README — дефолтный шаблон. Нет deploy-скриптов. |
| `wordpress/fl-radar-landing/` | **Мёртвый** | Версия 0.2.0, прототип, никогда не развивался. |
| `wordpress/node_modules/` | **Вес** | Закоммичен в git. Должен быть в `.gitignore`. |

---

## 8. SQL — дубликат номера

Два файла с префиксом `023`:
- `023_prepaid_subscription.sql` → **`025_prepaid_subscription.sql`** ✅ (A8)
- `023_user_tags_active.sql`

**Решение:** Переименовать один.

---

## ИТОГОВАЯ ТАБЛИЦА

| Категория | Кол-во | Экономия токенов |
|-----------|--------|-----------------|
| Дубли документов | 3 группы | ~500 строк контекста |
| Устаревшие документы | 6 файлов | ~4,000 строк контекста |
| Мёртвые документы | 3 файла | ~1,200 строк контекста |
| Мёртвый код (Python) | 3 файла | ~737 строк |
| Раздутые файлы (src/) | 5 файлов | ~13,898 строк (сплит) |
| Мусор в scripts/ | ~280 файлов | ~700 KB диска |
| Мёртвые проекты | 2 директории | ~15 MB диска |
| Секреты в открытом виде | 1 файл | **КРИТИЧНО** |

### Топ-5 действий по приоритету

1. **`.env` гигиена** — не ротация «на всякий»; см. [`FOR_YOU.md`](FOR_YOU.md) § `.env` · задачи **A0–A1** в [`TASKS.md`](team/common/TASKS.md)
2. **Архивировать OWNER_INTENT** — hot **311** строк ✅ (Lead A5, 2026-06-20)
3. **Удалить мёртвый `habr_freelance_parser.py`** — Coder **A7** (не трогать `process_guard` / `cursor_proxy_relay`)
4. **Очистить scripts/** — Coder **A10–A12**
5. **Сплит api_server.py** — backlog **A13**, не перед M1
