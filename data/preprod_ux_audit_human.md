# Pre-prod UX audit — human (O80 LLM)

- **Time:** 2026-06-07T10:52:45.751843+00:00
- **Model:** google/gemini-2.5-flash-lite
- **Mobile screenshots:** 12
- **Script critical count:** 13
- **U10b draft cards:** 0

---
## Критично

*   **U2 (mobile): Лента: категория + навыки.** Тайм-аут ожидания элемента `.rl-feed-sheet__panel .rl-feed-skill:not(.is-disabled), .rl-chip, .rl-feed-skills .rl-chip`. Это означает, что фильтры по навыкам не загружаются или не отображаются корректно на мобильных устройствах. Пользователь не сможет выбрать нужные навыки для фильтрации ленты.
*   **U3 (mobile): Сортировка + закрытие.** Отсутствует радиокнопка "По совместимости" (match) в мобильном листе фильтров. Это критично, так как пользователь не сможет применить этот важный способ сортировки.
*   **U4 (mobile): Expand + tap outside.** Ошибка `strict mode violation` при попытке найти `.rl-feed-card__body-inner` внутри карточки заказа. Локатор находит два элемента, что указывает на проблему с уникальностью или структурой DOM. Это может приводить к некорректному отображению или взаимодействию с карточками.
*   **U7 (mobile): ЛК: навыки + inbox.** Модальное окно навыков в личном кабинете не закрывается при нажатии на оверлей. Это нарушает стандартное поведение модальных окон и может привести к путанице или невозможности продолжить работу.
*   **U10 (desktop): Console + network.** Ошибки `429 Too Many Requests` в консоли и сетевых запросах. Это указывает на проблемы с сервером или ограничениями на количество запросов, что может влиять на доступность и производительность сервиса.
*   **U11 (mobile): Match breakdown.** Тайм-аут ожидания элемента `.rl-match`. Невозможно получить информацию о совпадениях, что является важной функцией для пользователя.
*   **U12 (mobile): Tools auth-only.** В платной ленте заказов отсутствует блок "Инструменты" на развернутых карточках. Это может быть связано с ограничениями доступа для неавторизованных пользователей или ошибкой в отображении.

## Раздражает

*   **U2 (desktop): Лента: категория + навыки.** Тайм-аут ожидания элемента `.rl-feed-skills-dd`. Аналогично мобильной версии, но на десктопе, что также мешает пользователю фильтровать ленту.
*   **U3 (desktop): Сортировка + закрытие.** Тайм-аут ожидания радиокнопки сортировки по совместимости. Пользователь не может применить сортировку.
*   **U4 (desktop): Expand + tap outside.** Аналогичная проблема с `strict mode violation` на десктопе, как и на мобильных устройствах.
*   **U10b (desktop): Draft×5 tools audit.** Только 0 из 3 черновиков прошли проверку, с ошибкой AI. Это указывает на проблемы с функционалом или контентом, связанным с черновиками и инструментами.
*   **U11 (desktop): Match breakdown.** Тайм-аут ожидания элемента `.rl-match`. Невозможность получить информацию о совпадениях на десктопе.
*   **U12 (desktop): Tools auth-only.** Отсутствие блока "Инструменты" на развернутых карточках в платной ленте на десктопе.

## Ок

*   На предоставленных скриншотах нет явных проблем с понятностью интерфейса, куда жать, перекрытиями или мелкими tap targets. Интерфейс выглядит достаточно стандартным для ленты заказов.

**Rating (1–5)**
2/5
